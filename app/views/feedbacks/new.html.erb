<h1>Kontakt</h1>

<h5>Masz pytanie?<br/>Napisz na adres <u>jacek@studium-zycia.pl</u> lub skorzystaj z formularza.</h5>

<%= form_for(@feedback) do |f| %>
  <% if @feedback.errors.any? %>
    <div id="error_explanation">
      <h2>Wiadomość nie została wysłana, ponieważ:</h2>

      <ul>
      <% @feedback.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username, 'E-mail:' %><br />
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :message, 'Treść:' %><br />
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit 'Wyślij' %>
  </div>
<% end %>
